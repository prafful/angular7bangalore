import { StringSortPipePipe } from './string-sort-pipe.pipe';

describe('StringSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
