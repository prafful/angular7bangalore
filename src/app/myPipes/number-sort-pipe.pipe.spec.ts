import { NumberSortPipePipe } from './number-sort-pipe.pipe';

describe('NumberSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
